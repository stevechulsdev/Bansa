<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stevechulsdev.bansa">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash.view.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Kakao -->
        <meta-data android:name="com.kakao.sdk.AppKey" android:value="dbaeb61adc86ec5a135e403a23a2b60e"/>
        <meta-data android:name="com.kakao.sdk.ClientSecret" android:value="5QAr7FU1AwhtPA7KdEIdBvNjHNFbQ3I5"/> <!-- kakao secret key -->

        <activity android:name="com.kakao.auth.authorization.authcode.KakaoWebViewActivity"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <!-- Login -->
        <activity
            android:name=".login.view.LoginActivity"
            android:theme="@style/TransparentActivity"/>

        <!-- Main -->
        <activity android:name=".main.view.MainActivity" />

    </application>

</manifest>